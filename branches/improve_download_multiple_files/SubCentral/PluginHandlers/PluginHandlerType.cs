
namespace SubCentral.PluginHandlers {
    public enum PluginHandlerType {
        UNDEFINED, // all
        BASIC,
        BASICWITHOUTDB, // doesn't have subtitle fields in internal db
        CUSTOM,
        MANUAL
    }
}